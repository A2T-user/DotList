<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Внутреннее хранилище приложения (filesDir) -->
    <files-path name="internal_files" path="." />

    <!-- Внешнее приватное хранилище (externalFilesDir) -->
    <external-files-path name="external_files" path="." />
</paths>